<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Dashboard</title>
</head>
<body>
    <h1>Welcome to the Travel Dashboard</h1>

<form action="/messages" method="POST">
  <input type="text" placeholder="Enter city" name="name" required>
  <input type="text" placeholder="Enter country" name="msg" required>
  <button type="submit">Save</button>
</form>

<ul>
  <% messages.forEach(m => { %>
    <li>
      <strong><%= m.name %></strong> - <%= m.msg %>
      <a href="/city/<%= m.name %>">View</a>
    </li>
  <% }) %>
</ul>
  <a href="/logout">Logout</a>

</body>
</html>
