<!DOCTYPE html>
<html>
<head>
  <title><%= city %> - Explore Echo</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container text-center">
    <h1>Welcome to <%= city.charAt(0).toUpperCase() + city.slice(1) %></h1>
    <img src="<%= data.img %>" alt="<%= city %>" class="img-responsive">
    <p><%= data.info %></p>
    <a href="/profile" class="btn btn-success">Back to Profile</a>
  </div>
</body>
</html>